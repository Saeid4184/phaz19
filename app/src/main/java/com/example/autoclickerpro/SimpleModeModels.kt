package com.example.autoclickerpro

/**
 * فاز ۱۱ — «حالت ساده»: یک لیستِ خامِ نقطه که فقط به‌ترتیب و با یک فاصله‌ی
 * زمانیِ ثابت کلیک می‌خورن، بدون هیچ شرط/تشخیصی (نه المان، نه تصویر، نه
 * OCR). برخلاف ClickRule که کلی فیلد اختیاری داره، این مدل عمداً حداقلیه —
 * دقیقاً همون چیزی که «حالت ساده» قراره باشه.
 */
data class SimplePoint(val x: Float, val y: Float)
